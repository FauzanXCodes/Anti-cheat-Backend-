mod damage_system {
    fn run(player: felt252, damage: u32) {
        let max_damage: u32 = 50;

        if damage > max_damage {
            // CHEAT DETECTED
            return ();
        }

        return ();
    }
}
