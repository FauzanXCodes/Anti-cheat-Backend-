mod movement_system {
    fn run(player: felt252, speed: u32) {
        let max_speed: u32 = 15;

        if speed > max_speed {
            // CHEAT DETECTED
            return ();
        }

        return ();
    }
}
