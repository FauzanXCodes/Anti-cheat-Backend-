%lang starknet

from dojo.world import WorldDispatcher

// Migration contract for Dojo 1.8
@external
func apply_migration(world: WorldDispatcher) {
    // You can add registrations here later
    return ();
}
